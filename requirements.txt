# Core runtime dependencies
openwakeword>=0.4.0
faster-whisper>=1.0.0
piper-tts>=1.3.0
onnxruntime>=1.10.0

# Audio
PyAudio>=0.2.13
sounddevice>=0.4.0
simpleaudio>=1.0.4
webrtcvad>=2.0.10

# LLM (local inference)
# Install ollama separately: https://ollama.com/download
# Or use llama-cpp-python for direct llama.cpp integration
# llama-cpp-python>=0.2.0

# ML / Torch (for training and GPU-accelerated inference)
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1

# Accessibility (Linux)
# pyatspi - install via: sudo apt install python3-pyatspi
# For Windows: pywinauto>=0.6.8
# For macOS: pyobjc-framework-Cocoa

# Browser extension IPC
websockets>=12.0

# NLP / Intent
spacy>=3.7.0

# Utilities
numpy>=1.21.6
scipy>=1.3,<1.15
scikit-learn>=1.0
pyyaml>=6.0
requests>=2.0
tqdm>=4.0

# Data / Storage
# sqlite3 is built into Python stdlib

# Development
pytest>=7.0
